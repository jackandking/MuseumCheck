# 排行榜更新机制说明

## 概述

MuseumCheck 的排行榜采用智能缓存机制，在保证数据新鲜度的同时减少服务器负载，提供最佳用户体验。

## 排行榜何时更新？

### 1. 自动更新时机

排行榜会在以下情况自动更新：

#### ✅ 打卡后立即提交
- 当您勾选博物馆为"已参观"时，系统会**自动提交**您的分数到排行榜服务器
- 提交内容包括：参观数量、积分(XP)、宠物战力
- 提交成功后，您的排名会在服务器端立即更新

#### ✅ 完成任务后自动更新
- 获得积分(XP)时自动提交
- 宠物升级时自动提交
- 所有影响排名的活动都会触发自动提交

#### ✅ 强制刷新窗口（3秒）
- 如果您在提交分数后的**3秒内**打开排行榜
- 系统会**自动跳过缓存**，直接从服务器获取最新数据
- 这确保您能立即看到最新的排名变化

### 2. 缓存机制

为了提升性能和减少服务器负载，排行榜使用**10分钟缓存**：

```
用户打开排行榜
  ↓
检查缓存是否过期？
  ├─ 是（超过10分钟） → 从服务器获取最新数据 → 显示 (最新)
  └─ 否（未满10分钟） → 使用缓存数据 → 显示 (缓存，X分钟后自动更新)
```

#### 缓存标识说明

在排行榜底部的"更新时间"显示：

- **`12/21 14:30 (最新)`** - 这是刚从服务器获取的最新数据
- **`12/21 14:30 (缓存，5分钟后自动更新)`** - 使用缓存数据，5分钟后自动过期
- **`12/21 14:30 (缓存已过期)`** - 缓存已过期但尚未刷新

#### ℹ️ 信息图标

更新时间旁边有一个 **ℹ️** 图标，鼠标悬停可查看说明：
- "排行榜自动缓存10分钟，点击刷新按钮可获取最新数据"
- "这是从服务器获取的最新数据"

### 3. 手动刷新

如果您想立即查看最新排名：

1. 点击排行榜底部的 **🔄 刷新排行榜** 按钮
2. 系统会绕过缓存，从服务器获取最新数据
3. 刷新成功后，更新时间会显示 `(最新)`

## 排行榜更新流程

### 用户操作流程

```
1. 用户参观博物馆并打卡
   ↓
2. 系统自动提交分数到服务器 ✓
   ↓
3. 服务器更新排行榜数据 ✓
   ↓
4. 用户打开排行榜查看
   ↓
5. 根据时间决定是否使用缓存
```

### 数据流向

```
本地数据 (localStorage)
  ↓
自动提交 (autoSubmitScore)
  ↓
远程服务器 (KV Store)
  ↓
排行榜数据 (10分钟缓存)
  ↓
显示给用户
```

## 常见问题解答

### Q1: 为什么我刚打卡但排行榜没有变化？

**A:** 可能有以下原因：

1. **使用了缓存数据** - 查看更新时间是否显示"(缓存)"
   - **解决方法**：点击 🔄 刷新排行榜按钮

2. **网络延迟** - 数据提交可能需要几秒钟
   - **解决方法**：等待几秒后再刷新

3. **多设备使用** - 如果在多个设备上使用，数据不会自动同步
   - **解决方法**：每个设备需要单独打开排行榜才能看到最新排名

### Q2: 排行榜多久更新一次？

**A:** 
- **您的分数提交**：打卡后立即提交到服务器
- **数据显示**：受10分钟缓存机制影响
- **推荐做法**：打卡后点击刷新按钮，立即看到最新排名

### Q3: 如何查看最新的排行榜？

**A:** 三种方式：

1. **刚打卡后** - 打卡后3秒内打开排行榜，自动显示最新数据
2. **手动刷新** - 点击排行榜底部的 **🔄 刷新排行榜** 按钮
3. **等待缓存过期** - 等待10分钟后重新打开排行榜

### Q4: 为什么要使用缓存？

**A:** 
- **提升性能** - 减少网络请求，排行榜打开更快
- **降低负载** - 减少服务器压力，确保稳定运行
- **优化体验** - 即使网络不佳也能查看排行榜

### Q5: 排行榜有哪些类型？

**A:** 目前支持三种排行榜：

1. **🏛️ 博物馆排行榜** - 按参观博物馆数量排名
2. **⭐ 积分排行榜** - 按完成任务获得的积分(XP)排名
3. **🐾 宠物排行榜** - 按虚拟宠物战斗力排名

每个排行榜独立计算和更新，可通过标签页切换查看。

## 技术实现细节

### 缓存管理

```javascript
// 缓存配置
this.cacheDuration = 10 * 60 * 1000;  // 10分钟
this.scoreSubmitGracePeriod = 3000;   // 3秒强制刷新窗口

// 缓存键
localStorage.getItem('leaderboardCache');       // 缓存数据
localStorage.getItem('leaderboardCacheExpiry'); // 过期时间
```

### 自动提交触发条件

分数会在以下情况自动提交：

1. **参观数量变化** - 勾选或取消勾选"已参观"
2. **积分(XP)变化** - 完成任务、上传照片获得积分
3. **宠物数据变化** - 宠物攻击力或防御力提升

```javascript
// 检查是否需要提交
shouldSubmitScore() {
    // 检查参观数量是否变化
    // 检查XP是否变化  
    // 检查宠物战力是否变化
    return hasChanges;
}
```

### 强制刷新逻辑

```javascript
// 检查是否在3秒强制刷新窗口内
shouldForceRefresh() {
    const timeSinceSubmit = Date.now() - this.lastScoreSubmitTime;
    return timeSinceSubmit < this.scoreSubmitGracePeriod; // 3000ms
}
```

## 数据存储位置

### 本地存储 (localStorage)
- 您的个人参观记录
- 任务完成状态
- 积分和宠物数据
- **排行榜缓存数据**（仅用于加速显示）

### 远程服务器 (KV Store)
- 全网所有用户的排行榜数据
- 使用复合键存储：`museumcheck-leaderboard`
- 每个用户一条记录：`user-{userId}`

### 隐私保护
- 排行榜仅显示：昵称、分数
- **不包含**：真实姓名、联系方式等个人信息

## 排名变化通知

系统会在打卡后自动检查排名变化，并显示通知：

### 排名提升
```
🎉 恭喜！排名上升了3位！
🏆 排行榜第5名（共120名用户）
```

### 首次上榜
```
🏆 首次登上排行榜！当前第8名
```

### 排名下降
这是正常现象！其他用户也在努力探索博物馆，继续加油！

## 最佳实践建议

### 对于家长
1. **及时打卡** - 参观完博物馆后及时勾选"已参观"
2. **完成任务** - 鼓励孩子完成任务，获取更多积分
3. **查看排名** - 打卡后点击刷新按钮，查看最新排名变化

### 对于孩子
1. **探索博物馆** - 多参观不同类型的博物馆
2. **完成任务** - 认真完成每个博物馆的任务清单
3. **培养宠物** - 喂养和训练虚拟宠物，提升战力

### 对于开发者
1. **缓存策略** - 10分钟缓存平衡了性能和数据新鲜度
2. **强制刷新** - 3秒窗口确保用户打卡后能立即看到变化
3. **自动提交** - 减少用户操作，提升体验
4. **错误处理** - 网络失败时使用缓存数据，确保可用性

## 未来改进计划

### 计划中的功能
- [ ] 推送通知：排名大幅提升时发送通知
- [ ] 排名历史：查看排名变化趋势图表
- [ ] 好友对比：与好友排名对比
- [ ] 实时更新：WebSocket实时推送排名变化

### 性能优化
- [ ] 增量更新：仅更新变化的排名数据
- [ ] 分区缓存：按地区或类型分组缓存
- [ ] 预加载：在后台预加载排行榜数据

## 支持与反馈

如有任何问题或建议，欢迎通过以下方式联系：

- **GitHub Issues**: [提交问题](https://github.com/jackandking/MuseumCheck/issues)
- **GitHub Discussions**: [参与讨论](https://github.com/jackandking/MuseumCheck/discussions)

感谢使用博物馆打卡！祝您和孩子享受愉快的博物馆探索之旅！🏛️✨
